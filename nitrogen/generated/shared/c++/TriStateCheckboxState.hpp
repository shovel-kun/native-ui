///
/// TriStateCheckboxState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nativeui {

  /**
   * An enum which can be represented as a JavaScript union (TriStateCheckboxState).
   */
  enum class TriStateCheckboxState {
    ON      SWIFT_NAME(on) = 0,
    OFF      SWIFT_NAME(off) = 1,
    INDETERMINATE      SWIFT_NAME(indeterminate) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nativeui

namespace margelo::nitro {

  using namespace margelo::nitro::nativeui;

  // C++ TriStateCheckboxState <> JS TriStateCheckboxState (union)
  template <>
  struct JSIConverter<TriStateCheckboxState> final {
    static inline TriStateCheckboxState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("on"): return TriStateCheckboxState::ON;
        case hashString("off"): return TriStateCheckboxState::OFF;
        case hashString("indeterminate"): return TriStateCheckboxState::INDETERMINATE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum TriStateCheckboxState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, TriStateCheckboxState arg) {
      switch (arg) {
        case TriStateCheckboxState::ON: return JSIConverter<std::string>::toJSI(runtime, "on");
        case TriStateCheckboxState::OFF: return JSIConverter<std::string>::toJSI(runtime, "off");
        case TriStateCheckboxState::INDETERMINATE: return JSIConverter<std::string>::toJSI(runtime, "indeterminate");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert TriStateCheckboxState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("on"):
        case hashString("off"):
        case hashString("indeterminate"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
