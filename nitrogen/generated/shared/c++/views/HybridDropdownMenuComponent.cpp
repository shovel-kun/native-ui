///
/// HybridDropdownMenuComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridDropdownMenuComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nativeui::views {

  extern const char HybridDropdownMenuComponentName[] = "DropdownMenu";

  HybridDropdownMenuProps::HybridDropdownMenuProps(const react::PropsParserContext& context,
                                                   const HybridDropdownMenuProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    label([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("label", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.label;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.label);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("DropdownMenu.label: ") + exc.what());
      }
    }()),
    selectedIndex([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("selectedIndex", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.selectedIndex;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.selectedIndex);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("DropdownMenu.selectedIndex: ") + exc.what());
      }
    }()),
    onOptionSelected([&]() -> CachedProp<std::optional<std::function<void(double /* index */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onOptionSelected", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onOptionSelected;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(double /* index */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onOptionSelected);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("DropdownMenu.onOptionSelected: ") + exc.what());
      }
    }()),
    options([&]() -> CachedProp<std::vector<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("options", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.options;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::vector<std::string>>::fromRawValue(*runtime, value, sourceProps.options);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("DropdownMenu.options: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::nativeui::HybridDropdownMenuSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::nativeui::HybridDropdownMenuSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("DropdownMenu.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridDropdownMenuProps::HybridDropdownMenuProps(const HybridDropdownMenuProps& other):
    react::ViewProps(),
    label(other.label),
    selectedIndex(other.selectedIndex),
    onOptionSelected(other.onOptionSelected),
    options(other.options),
    hybridRef(other.hybridRef) { }

  bool HybridDropdownMenuProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("label"): return true;
      case hashString("selectedIndex"): return true;
      case hashString("onOptionSelected"): return true;
      case hashString("options"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridDropdownMenuComponentDescriptor::HybridDropdownMenuComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  react::Props::Shared HybridDropdownMenuComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                         const react::Props::Shared& props,
                                                                         react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridDropdownMenuShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridDropdownMenuComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridDropdownMenuShadowNode&>(shadowNode);
    const HybridDropdownMenuProps& props = concreteShadowNode.getConcreteProps();
    HybridDropdownMenuState state = concreteShadowNode.getStateData();

    auto width = state._width;
    auto height = state._height;

    if (!isnan(width) or !isnan(height)) {
        auto const &props = *std::static_pointer_cast<const facebook::react::ViewProps>(concreteShadowNode.getProps());

        // The node has width and/or height set as style props, so we should not override it
        auto widthProp = props.yogaStyle.dimension(facebook::yoga::Dimension::Width);
        auto heightProp = props.yogaStyle.dimension(facebook::yoga::Dimension::Height);

        if (widthProp.value().isDefined()) {
            // view has fixed dimension size set in props, so we should not autosize it in that axis
            width = widthProp.value().unwrap();
        }
        if (heightProp.value().isDefined()) {
            height = heightProp.value().unwrap();
        }

        concreteShadowNode.setSize({width, height});
    }
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nativeui::views
