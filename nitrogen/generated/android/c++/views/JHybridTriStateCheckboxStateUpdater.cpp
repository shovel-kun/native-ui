///
/// JHybridTriStateCheckboxStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridTriStateCheckboxStateUpdater.hpp"
#include "views/HybridTriStateCheckboxComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JNISharedPtr.hpp>

namespace margelo::nitro::nativeui::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridTriStateCheckboxState>;

void JHybridTriStateCheckboxStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridTriStateCheckboxSpec::javaobject> javaView,
                                           jni::alias_ref<react::StateWrapperImpl::javaobject> stateWrapper) {
  JHybridTriStateCheckboxSpec* view = javaView->cthis();
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridTriStateCheckboxState& data = concreteState->getData();
  const std::optional<HybridTriStateCheckboxProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridTriStateCheckboxState's data doesn't contain any props!");
  }
  const HybridTriStateCheckboxProps& props = maybeProps.value();
  if (props.state.isDirty) {
    view->setState(props.state.value);
    // TODO: Set isDirty = false
  }
  if (props.disabled.isDirty) {
    view->setDisabled(props.disabled.value);
    // TODO: Set isDirty = false
  }
  if (props.onPress.isDirty) {
    view->setOnPress(props.onPress.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      auto shared = JNISharedPtr::make_shared_from_jni<JHybridTriStateCheckboxSpec>(jni::make_global(javaView));
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::nativeui::views
