///
/// JHybridTextInputStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridTextInputStateUpdater.hpp"
#include "views/HybridTextInputComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JNISharedPtr.hpp>

namespace margelo::nitro::nativeui::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridTextInputState>;

void JHybridTextInputStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridTextInputSpec::javaobject> javaView,
                                           jni::alias_ref<react::StateWrapperImpl::javaobject> stateWrapper) {
  JHybridTextInputSpec* view = javaView->cthis();
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridTextInputState& data = concreteState->getData();
  const std::optional<HybridTextInputProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridTextInputState's data doesn't contain any props!");
  }
  const HybridTextInputProps& props = maybeProps.value();
  if (props.value.isDirty) {
    view->setValue(props.value.value);
    // TODO: Set isDirty = false
  }
  if (props.onValueChange.isDirty) {
    view->setOnValueChange(props.onValueChange.value);
    // TODO: Set isDirty = false
  }
  if (props.placeholder.isDirty) {
    view->setPlaceholder(props.placeholder.value);
    // TODO: Set isDirty = false
  }
  if (props.label.isDirty) {
    view->setLabel(props.label.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      auto shared = JNISharedPtr::make_shared_from_jni<JHybridTextInputSpec>(jni::make_global(javaView));
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::nativeui::views
