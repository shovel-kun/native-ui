///
/// HybridTextInputComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridTextInputComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridTextInputSpecSwift.hpp"
#import "NativeUi-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::nativeui;
using namespace margelo::nitro::nativeui::views;

/**
 * Represents the React Native View holder for the Nitro "TextInput" HybridView.
 */
@interface HybridTextInputComponent: RCTViewComponentView
@end

@implementation HybridTextInputComponent {
  std::shared_ptr<HybridTextInputSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridTextInputComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridTextInputComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridTextInputSpec> hybridView = NativeUi::NativeUiAutolinking::createTextInput();
    _hybridView = std::dynamic_pointer_cast<HybridTextInputSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NativeUi::HybridTextInputSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const react::Props::Shared&)props
            oldProps:(const react::Props::Shared&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridTextInputProps const>(props);
  auto& newViewProps = const_cast<HybridTextInputProps&>(newViewPropsConst);
  NativeUi::HybridTextInputSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // defaultValue: optional
  if (newViewProps.defaultValue.isDirty) {
    swiftPart.setDefaultValue(newViewProps.defaultValue.value);
    newViewProps.defaultValue.isDirty = false;
  }
  // onChangeText: optional
  if (newViewProps.onChangeText.isDirty) {
    swiftPart.setOnChangeText(newViewProps.onChangeText.value);
    newViewProps.onChangeText.isDirty = false;
  }
  // multiline: optional
  if (newViewProps.multiline.isDirty) {
    swiftPart.setMultiline(newViewProps.multiline.value);
    newViewProps.multiline.isDirty = false;
  }
  // numberOfLines: optional
  if (newViewProps.numberOfLines.isDirty) {
    swiftPart.setNumberOfLines(newViewProps.numberOfLines.value);
    newViewProps.numberOfLines.isDirty = false;
  }
  // keyboardType: optional
  if (newViewProps.keyboardType.isDirty) {
    swiftPart.setKeyboardType(newViewProps.keyboardType.value);
    newViewProps.keyboardType.isDirty = false;
  }
  // autocorrection: optional
  if (newViewProps.autocorrection.isDirty) {
    swiftPart.setAutocorrection(newViewProps.autocorrection.value);
    newViewProps.autocorrection.isDirty = false;
  }
  // variant: optional
  if (newViewProps.variant.isDirty) {
    swiftPart.setVariant(newViewProps.variant.value);
    newViewProps.variant.isDirty = false;
  }
  // placeholder: optional
  if (newViewProps.placeholder.isDirty) {
    swiftPart.setPlaceholder(newViewProps.placeholder.value);
    newViewProps.placeholder.isDirty = false;
  }
  // label: optional
  if (newViewProps.label.isDirty) {
    swiftPart.setLabel(newViewProps.label.value);
    newViewProps.label.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
