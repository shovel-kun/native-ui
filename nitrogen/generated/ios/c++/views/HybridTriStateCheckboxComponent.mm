///
/// HybridTriStateCheckboxComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridTriStateCheckboxComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridTriStateCheckboxSpecSwift.hpp"
#import "NativeUi-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::nativeui;
using namespace margelo::nitro::nativeui::views;

/**
 * Represents the React Native View holder for the Nitro "TriStateCheckbox" HybridView.
 */
@interface HybridTriStateCheckboxComponent: RCTViewComponentView
@end

@implementation HybridTriStateCheckboxComponent {
  std::shared_ptr<HybridTriStateCheckboxSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridTriStateCheckboxComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridTriStateCheckboxComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridTriStateCheckboxSpec> hybridView = NativeUi::NativeUiAutolinking::createTriStateCheckbox();
    _hybridView = std::dynamic_pointer_cast<HybridTriStateCheckboxSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NativeUi::HybridTriStateCheckboxSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const react::Props::Shared&)props
            oldProps:(const react::Props::Shared&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridTriStateCheckboxProps const>(props);
  auto& newViewProps = const_cast<HybridTriStateCheckboxProps&>(newViewPropsConst);
  NativeUi::HybridTriStateCheckboxSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // state: enum
  if (newViewProps.state.isDirty) {
    swiftPart.setState(static_cast<int>(newViewProps.state.value));
    newViewProps.state.isDirty = false;
  }
  // disabled: optional
  if (newViewProps.disabled.isDirty) {
    swiftPart.setDisabled(newViewProps.disabled.value);
    newViewProps.disabled.isDirty = false;
  }
  // onPress: optional
  if (newViewProps.onPress.isDirty) {
    swiftPart.setOnPress(newViewProps.onPress.value);
    newViewProps.onPress.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
